# Review fix task

Fix review findings in the existing human-atlas-tcm clone. Preserve user data and architecture. Own app/tcm/TcmApp.tsx, StudyPanel.tsx, study.ts, types.ts, data.ts only if source metadata required, tests covering fixes, docs/content-sources.md count. Do not change other files without explaining necessity. Root is testing browser/mobile independently, no concurrent edits to these files.

1. Quiz feedback disappears in review scope. TcmApp.tsx schedules target immediately, filtered removes it, filter effect auto advances/clears answer or empty detail hides feedback. Preserve answered target+feedback through explicit 下一题 even if no remaining due cards. Skip has same issue. Write meaningful pure state regression tests if extract helper; if UI test only provide reproduction for root CUA. Do not expose all previously studied cards as due. Ensure 下一题 goes to next actual due item; finished queue has actionable empty state. User changing filters/mode can intentionally clear answer.
2. Unrevealed 归经 cards leak answer via stage label, floating selection, sidebar rows/filter options. Apply question-specific concealment to all visible UI and accessibility until reveal. Point name may remain question, but hide meridian/code hints and already-active filtered meridian name. Identification already hides name; retain it and keep type across cards. For location questions model highlighter already gives spatial hint but quiz separate, don't expand scope.
3. Classification card answer-source always GB location standard. Add typed field or selector to choose actual source by question type. Data has classSources map covering certain IDs. Ensure verified classification source is used only when it directly supports given displayed tags, else explicitly pending. Do not pretend naming standard proves classifications. If source supports subset of tags distinguish pending remainder. Source scope integrity matters more than blanket source fallback.
4. docs/content-sources stale crosschecked count 8 vs actual14 fix.

Add minimal meaningful regression tests; run npm test + npm run check. Existing full tests already pass, keep them. Commit scoped fixes. Write docs/fix-report.md with fixes/tests and concise status. Do not spawn subagents. Use apply_patch. No browser tools needed; root will verify UI reproductions.
