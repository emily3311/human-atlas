# 验证记录

## 第二轮扩充与设计更新（2026-09-06）

- 米白底、暖橙强调、深棕文字，中性灰体表；卡片、导航、目录、提示与阴影统一换色。
- 23个测试通过：新增362国标目录、383学习条目、考纲90/180范围、缺失资料题型禁用、手足背分类及解剖散开变换回归。
- TypeScript、生产构建、模型资源与交互契约、静态服务检查通过。
- 浏览器实测：一键散开、胫骨名称搜索、单独放大、恢复经穴学习；孔最搜索与定位卡翻面；三角灸待核验提示及练习禁用；实践技能/医学综合筛选；390×844窄屏无横向溢出。未写入示范复习成绩。
- 浏览器错误日志为空。新增条目不猜测三维位置或临床答案。下面保留首版记录，范围差异以第二轮文档和README为准。

# 首版验证记录（历史）

日期：2026-09-06。范围：本地中文教学扩展，不代表医学精度认证。

## 自动检查

- `npm test`：14/14 通过，涵盖数据结构、来源支持范围、复习调度、损坏存储恢复、空队列导航和答题反馈保留。
- `npm run check`：TypeScript 检查通过。
- `node scripts/validate-atlas.mjs`：2234 个网格、3432 个概念映射、2,288,268 个三角形及二进制缓冲区通过。
- `node scripts/validate-interactions.mjs`：桌面/手机比例、点按、拖动、多触控、取消、空视图契约通过。
- `npm run build`：生产构建通过；仍有大于 500 kB 的脚本包提示，未来可按模块拆包。
- `node --check server.mjs`、`zsh -n START_MAC.command`、`git diff --check`：通过。
- `node scripts/validate-server.mjs`：先复现主页错误返回 403，再修复目录末尾斜杠归一化；主页、脚本、模型、压缩响应头、越界路径、缺失文件和不支持方法全部通过。

## 浏览器实测

- 桌面 1280×800 和手机 390×844：人体加载、布局、手机纵向滚动、固定导航及目录入口。
- 中文/编码与无声调拼音搜索：`hegu` 正确筛出合谷；空筛选可恢复。
- 体表与骨骼切换、邻近解剖结构高亮、左胫骨单独显示/恢复；隔离模式随视口调整保持局部取景。
- 足三里三步定位辅助与 16 等分示意；明确提示比例端点待校准。
- 定位卡翻面、掌握程度打分、下一张；认穴卡隐藏名称；归经卡隐藏编码和各处归经提示。
- 分类卡展示实际分类来源，未支持分类保留待核验状态。
- 全部范围自测错答反馈；待复习范围只剩一题时，查看答案后反馈保持，点击下一题才显示完成状态。
- 足三里/丰隆并排对比和关闭对话框。
- 添加课程穴位、输入笔记、JSON 导出，并检查导出内容；刷新后本地课程、笔记和学习进度保留。
- 导入字段校验已通过单元测试；文件选择器导入未做端到端实测。
- 情境练习选择答案、提交、解析和下一题入口。

浏览器测试记录使用 `127.0.0.1` 来源。用户使用 `localhost` 来源，避免带入演示答题成绩。

正式静态服务已在 `http://localhost:3016/` 启动并通过浏览器检查：人体、目录和详情正常加载，初始进度 0/39；错误日志为空。

## 审查及范围差异

独立代码审查发现的三项重要问题（复习答题反馈丢失、归经卡泄露答案、分类证据错用定位标准）已修复，并通过定向复审。

相较初始计划，新教学视图没有拆解/爆炸功能；原始英文解剖视图保留。课程笔记没有逐条专业审核状态，只有本地个人记录。完整经络循行、全量经穴、女性/多体位模型、临床病例库和针灸操作仿真未实现。详见 README-TCM.md。

39 穴中的传统功用有 14 穴获得所列机构正文交叉支持，25 穴仍待逐条核验；全部均需课程教师审校。三维穴位位置及比例尺端点均未专业校准，不能据此在真人身上针刺或施灸。
