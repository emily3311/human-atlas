# Emily AI 项目入口验收

日期：2026-09-06。用户批准“顶部常驻入口＋关于窗口介绍＋页脚补充”的设计。

## 已实现

- 顶部「更多 AI 项目 ↗」位于「我的学习」旁、学习导航之外，打开 `https://emilyailab.com/`。
- 原「资料与署名」窗口新增独立「认识 Emily AI」介绍，含官网与 `https://emilyailab.com/consulting` 咨询入口。
- 页脚增加「更多项目 · Emily AI ↗」。
- 四个新链接均为原生 anchor，带 `target="_blank"` 和 `rel="noopener noreferrer"`，不增加跟踪、弹出广告或模型覆盖卡片。
- 原 Human Atlas、BodyParts3D、CMB 及医学资料署名链接保持不变；不修改题库、穴位坐标、模型数据、学习存储及 Claude Design 交接材料。

实现提交：`104d64a`。响应式修正：`7d444eb`；平板目录偏移修正：`73518d6`。

## 测试和复核

- 新增真实 React 静态渲染测试，断言链接数量、目的地址、外链安全属性及介绍内容。先缺失组件、再临时空渲染的两轮 RED 均失败；实际实现后两项测试通过。
- 控制器在最终 CSS 修正后重新运行：全套 60 / 60 测试、TypeScript 检查和 `git diff --check` 通过。
- 实现代理构建通过，仍有既有的主包大于 500 kB 的体积建议，不是构建失败。
- 任务复查发现窄屏 grid 自动排版把项目链接推到第三行；定向修复为品牌第 1 行第 1 列、链接第 1 行第 2 列、导航第 2 行跨列。定向复查通过。
- 最终复查另发现 681–900 px 的经穴目录仍沿用 88 px 顶部偏移；补充仅限非解剖模式的 113 px 偏移。实测 768 px 时目录顶部与页头底部均为 113 px，开关正常，不再盖住导航。最后 CSS 修正后全套 60 / 60 测试再次通过。
- 最终定向复查确认问题已处理、修正未引入新问题。本功能没有剩余阻塞项；之前记录的 CMB 小项不受本轮影响。

## 实际浏览器结果

现有本地站点：`http://localhost:3016/`，Chrome。正常视口为 1512 × 750；临时尺寸覆盖已撤销。

| 视口宽度 | 页头高度 | 官网链接位置 | 页面横向溢出 |
| --- | --- | --- | --- |
| 1512 | 88 | 顶部右侧，学习进度旁 | 无 |
| 1280 | 88 | 顶部右侧，学习进度旁 | 无 |
| 1024 | 88 | 顶部右侧，原学习进度按已有规则隐藏 | 无 |
| 768 | 113 | 品牌右侧，第 1 行，y=19–49 | 无 |
| 390 | 113 | 品牌右侧，第 1 行，y=19–49 | 无 |

- 查看桌面及手机截图确认新按钮不遮挡内容；手机保留完整按钮文案，页脚项目链接仍可见。
- 手机导航可横向滚动到最后一项「情境练习」，并能返回「解剖图谱」。
- 手机模型滑块从 0% 到 10% 再回到 0% 正常。
- 关于窗口实际呈现介绍及两个新链接，原六个来源/署名链接保留。
- 实际点击顶部链接打开了新的 Emily AI 官网标签，原 atlas 标签仍在 localhost，学习进度保留 2 / 382；没有答题、清空记录或修改用户数据。
- 浏览器错误日志为空。验收后恢复正常视口和解剖视图。
- 外链验收创建的临时官网标签已关闭；用户原有官网标签未动。

本轮没有合并、推送或线上部署；这份记录不意味着其余中医内容与术语核对工作已全部完成。
