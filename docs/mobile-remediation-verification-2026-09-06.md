# 移动端修复与内容核查验收记录

日期：2026-09-06。代码范围：`0734735..9d8b053`；预览：`http://localhost:3016/`。本记录只描述本轮实际交付，不代表整个医学教学工具已完成。

## 已交付

- 手机解剖页将操作栏、人体画布、相机栏、滑块分区；相机和滑块不再遮住画布。连续 0–100% 散开保持可用，滑块可收起；选中名称条压缩到单行，详情和目录按需打开。
- 增加应用内专注模型模式和明确退出入口；Escape 可退出。修复跨屏幕宽度切换后空白、桌面滑块无法恢复，以及专注模式中打开目录/详情却不可见的问题。
- 手机记忆卡、课堂、情境练习首屏先显示学习内容，模型默认折叠且不创建三维画布；执医题库在所有尺寸都不创建三维画布。展开/折叠或普通尺寸切换不重置卡片状态。
- 解剖目录搜索置顶、系统筛选默认折叠、结果独立滚动；手机抽屉使用可用高度。增加正文与主要触控区域尺寸，保留米白、橙色、深棕和 Emily AI 链接。
- 场景卸载取消下载并停止后续块调度；失败时取消并发请求，迟到的解码结果不再分配 GPU 资源。重试不刷新整站、不清空学习记录。未新增无界内存缓存；重复访问是否命中缓存仍取决于浏览器。
- 新增 22 个精确术语核心，覆盖 44 个实际模型条目：中文名称覆盖从 548 增至 592 / 2234，剩余 1642。依据为逐页核对的术语来源，不等同于专业逐结构审校。详见 `docs/anatomy-back-brain-evidence-2026-09-06.md`。

## 自动检查

在最终代码 `9d8b053` 上重新执行，均退出 0：

| 检查 | 结果 |
| --- | --- |
| `npm test` | 70 / 70 通过 |
| `npm run check` | TypeScript 无错误 |
| `npm run build` | 生产构建通过；仍有既有大于 500 kB 的打包体积警告 |
| `node scripts/validate-atlas.mjs` | 2234 网格、3432 概念映射、全部二进制缓冲区通过 |
| `node scripts/validate-interactions.mjs` | 排列、搜索/查看、点击/拖动/多点/取消/空视图契约通过 |
| `node scripts/validate-server.mjs` | 首页、资源、模型响应头、路径/方法边界通过 |
| `git diff --check` | 无空白错误 |

新增测试先验证失败，再实施；覆盖移动布局策略、跨尺寸焦点/滑块状态、下载取消/失败重试、精确术语与来源及实际覆盖率。

## 浏览器验收

在桌面内置浏览器中模拟尺寸，不是真机触摸测试。

- 320×568、375×844、390×844、430×844、768×844、1280×844、1512×844，各检查解剖图谱、经穴图谱、记忆卡片、取穴自测、执医题库、我的课堂、情境练习，共 49 组：未出现整页横向溢出。
- 手机记忆卡/课堂/情境在默认状态画布数量为 0，任务显示在首屏；题库在所有尺寸为 0。其余需要模型的模式保留画布。
- 390×844 解剖页：人体画布高 535 px，相机栏位于画布外，底部控制区 94 px。320×568 画布高 259 px；选中结构后名称条 54 px，仍有 201 px 画布，关闭按钮不换行。
- 实际验证卡片翻面及 390→768→390 切换后保持翻面状态；辨认卡的模型展开入口不显示穴名。未声称做完整屏幕阅读器无障碍审计。
- 题库以长题干查询得到匹配题，题干与 A–E 选项正常换行；只搜索/阅读，未作答或提交。
- 目录搜索“视神经”并选择“左视神经”正常；最终构建搜索“中央前回”得到左右两项，选中后详情标题为“左中央前回”。
- 手机经穴选择相关结构后，“单独查看”仍可切换为“显示周围”；解剖模式将该操作保留在详情中。
- 验证手机折叠滑块后切到桌面仍可用；桌面专注记忆卡切到手机能返回学习内容；专注状态打开目录会退出专注并显示目录。
- 最终构建额外检查 1280×720 的 100% 散开视图，无强制右侧卡片；Home 恢复 0%。390×844 普通人体完整可见；专注模式 Escape 退出正常。
- 49 组切换之后以及最终重载后的浏览器错误日志均为空。已取消测试视口覆盖，恢复用户正常窗口，保留解剖页预览。
- 未评分记忆卡、未提交考试答案、未清空/导入/迁移用户记录；原有学习进度未改写。

## 独立复核

移动实现经过任务复核和三轮定向修正，精确术语任务复核通过；最终对 `0734735..9d8b053` 全范围复核未发现 Critical/Important 问题。唯一文档状态过期问题已更新。复核为源码审查，浏览器和命令检查由主任务另行执行。

## 尚未完成，不能宣称已解决

1. 1642 个结构中文名称仍需逐项找可靠对应，不能仅删除“待校对”文字。
2. 383 个目录条目不等于 383 个已校准三维标记。当前只有 39 个近似三维定位数据；完整模型配准和逐点专业校准未完成，本轮没有编造坐标。
3. 保留 4086 道 CMB 单选题及原始答案，本轮未新增有可靠依据的解析；3 个原创情境练习不是官方考试题。现有题量不代表完整考试题型/全科覆盖。
4. 本轮额外来源排查未得到可直接合法导入、带可靠解析的新增题库；部分数据只有题目/答案，部分授权未能核实。没有绕过付费/登录或批量复制不明授权内容。
5. 未用 iPhone/Android 真机测试软键盘、触摸手势、内存、帧率、弱网/4G 下载时间；尺寸模拟通过不等于真机性能通过。
6. 服务仍仅监听本机 `127.0.0.1`。手机不能通过自己的 `localhost:3016` 访问此电脑；本轮未公网部署或开放局域网监听。

改动已保存在本地分支 `codex/tcm-learning-atlas`，未推送、合并或部署。用户原有设计交接等未提交文件保持不变。
